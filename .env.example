# ============================
# Example Environment Settings
# ============================
# Copy this file to `.env` and adjust values as needed.
# All values here are safe defaults and contain no sensitive data.
# You can tweak these for performance tuning or custom deployment.

# Load Balancer Config
LOAD_BALANCER_IP=127.0.0.1
LOAD_BALANCER_PORT=9000

# File Transfer Settings
BUFFERSIZE=524288               # 512 KB buffer size (512 * 1024)
MAX_CONNECTIONS=50
THREAD_POOL_SIZE=5
MAX_RETRIES=3

# Network Timeouts (in milliseconds)
RECV_TIMEOUT_MS=5000
SEND_TIMEOUT_MS=5000

# Peer Scoring Algorithm Parameters
SCORE_ALPHA=0.20
SCORE_BETA=0.80
THRESHOLD_K=1.25

# Tracker Config
TRACKER_INFO_PATH=tracker_info.txt

# Heartbeat Settings
HEARTBEAT_INTERVAL_SEC=10
HEARTBEAT_TIMEOUT_SEC=5

# Tracker Communication Timeouts (in milliseconds)
TRACKER_RECV_TIMEOUT_MS=5000
TRACKER_SEND_TIMEOUT_MS=5000
